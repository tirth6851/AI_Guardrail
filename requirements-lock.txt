# Exact-version lockfile for AI Guardrail's runtime + test dependencies.
# requirements.txt pins MINIMUMS ("groq>=1.5.0"); this file pins the EXACT
# versions verified working together in this environment (Python 3.11.9,
# Windows), for reproducible installs (`pip install -r requirements-lock.txt`).
#
# HOW THIS WAS GENERATED: this dev machine's global site-packages also has
# unrelated projects installed (Azure SDKs, Flask, PyInstaller, markitdown,
# the rejected torch/transformers research path, etc.) — a raw `pip freeze`
# here would pollute this lockfile with packages this project never imports.
# Instead, this list is curated: requirements.txt's 9 direct dependencies,
# plus their actual transitive dependencies as reported by `pip show`,
# checked against this project's own imports (guardrail/, api.py, cli.py,
# tests/). It is NOT machine-generated by a single command, so if a direct
# dependency's transitive graph changes on a future `pip install --upgrade`,
# this file needs a manual re-check — see "Known Issues & TODO" in CLAUDE.md.
#
# For a fully mechanical lockfile (recommended before a real deployment):
# create a clean venv with ONLY requirements.txt installed, then
#   pip freeze > requirements-lock.txt
# in that clean venv. This file is a best-effort curated equivalent because
# no clean venv was available in this session.

# --- direct runtime dependencies ---
groq==1.5.0
python-dotenv==1.1.0
scikit-learn==1.9.0
pandas==2.2.3
joblib==1.5.3
fastapi==0.115.12
uvicorn==0.34.2

# --- direct test dependencies ---
pytest==8.3.5
httpx==0.28.1

# --- transitive: groq ---
anyio==4.8.0
distro==1.9.0
httpcore==1.0.7
jiter==0.8.2
pydantic==2.10.6
pydantic_core==2.27.2
annotated-types==0.7.0
sniffio==1.3.1
typing_extensions==4.15.0

# --- transitive: scikit-learn / pandas ---
numpy==2.2.3
scipy==1.17.1
threadpoolctl==3.6.0
narwhals==2.23.0
python-dateutil==2.9.0.post0
pytz==2025.1
tzdata==2025.2
six==1.17.0

# --- transitive: fastapi / uvicorn ---
starlette==0.46.2
click==8.4.2
h11==0.14.0

# --- transitive: pytest ---
pluggy==1.6.0
iniconfig==2.1.0
packaging==24.2

# --- transitive: httpx (also a direct test dependency above) ---
certifi==2025.1.31
idna==3.10

# --- transitive: pytest (colorama is a Windows-only pytest dependency) ---
colorama==0.4.6
